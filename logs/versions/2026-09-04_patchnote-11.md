# 2026-09-04 — PatchNote 11

## Arquivos alterados
- `docs/README.md`
- `docs/CLAUDE.md`
- `docs/FIREBASE_SETUP.md`

## Itens/campos modificados
- Removida a instrução operacional de executar `firebase init functions` quando `firebase.json` já existe.
- Atualizado o fluxo de publicação das Functions com `firebase use` e `firebase deploy --only functions`.
- Documentado que as rotas `calendario/`, `banco-horas/` e `splash/` são geradas no artefato do GitHub Pages e não devem ser versionadas.
- Atualizada a estrutura do projeto com `firebase.json` e `functions/`.
- Atualizado o checklist para validar as rotas sem extensão `.html`.
