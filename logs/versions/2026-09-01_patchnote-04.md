# 2026-09-01 — PatchNote 04

## Arquivos alterados
- `assets/js/script.js`
- `assets/timewallet_icon.svg`
- `docs/CLAUDE.md`
- `docs/FIREBASE_SETUP.md`
- `docs/README.md`

## Itens/campos modificados
- corrigido bug: localização capturada em `capturarLocalizacao()` nunca era persistida de volta no registro salvo (registro gravava com `localizacao: null` antes da geolocalização resolver, e depois só atualizava o texto na tela). Nova função `salvarLocalizacaoRegistroHoje()` grava a localização no registro assim que a geolocalização/endereço resolvem, tanto no fluxo de galeria quanto no de câmera
- `docs/FIREBASE_SETUP.md`: regra de exemplo do Firestore corrigida — cobria só `users/{uid}/registros/{docId}`, faltando `users/{uid}/baixas/{docId}`; sem regra pra `baixas` o Firestore nega por padrão (`Missing or insufficient permissions`), quebrando o carregamento do Banco de Horas mesmo autenticado. Regra agora recursiva (`{document=**}`), cobre as duas subcoleções
- auditoria de segurança no app: confirmado que todo texto externo/de terceiro renderizado via `innerHTML` (endereço de geocoding OSM, nome/e-mail/foto da conta Google) passa por `escapeHtml()` antes — sem XSS encontrado; nomes de arquivo no ZIP são gerados internamente, sem risco de path traversal; `config.js` nunca commitado
- `assets/timewallet_icon.svg`: redesign do ícone do app, paleta ajustada pro tom verde-acinzentado (`#656D55`/`#59614A`) em vez do verde-oliva original
