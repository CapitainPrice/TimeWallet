# 2026-09-01 — PatchNote 07

## Arquivos alterados
- `assets/js/banco-horas.js`
- `docs/CLAUDE.md`
- `index.html`
- `views/banco-horas.html`
- `views/base.html`
- `assets/js/shared.js`
- `assets/css/style.css`

## Itens/campos modificados
- Banco de Horas: navegação de período (Histórico de registros) e select de "Comprovantes do período" agora só listam períodos do ano com pelo menos 1 registro, mais o período atual (sempre incluído, mesmo sem registro ainda, pra não deixar a lista vazia). Cálculo dos 12 períodos do ano continua existindo por trás (`getPeriodosDoAno`); só a lista exibida filtra (`getPeriodosComRegistro`/`periodoTemRegistro`). Saldo acumulado do topo não muda de valor
- redesign do modal de câmera (`cam-topbar`, `cam-close-btn` com botão X, `cam-frame`, `cam-shutter-btn`) em `index.html`, `views/banco-horas.html`, `views/base.html` e CSS correspondente; `shared.js` liga o novo botão de fechar (`camFecharX`)
