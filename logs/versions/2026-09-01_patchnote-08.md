# 2026-09-01 — PatchNote 08

## Arquivos alterados
- `assets/js/script.js`

## Itens/campos modificados
- corrigido bug: ao recarregar a home com o registro de hoje já salvo mas sem `localizacao` (tentativa anterior falhou), o card ficava travado no texto "Obtendo localização..." sem nenhuma tentativa real rodando por trás — nunca resolvia, nunca dava erro, nunca oferecia retry. `carregarRegistroHoje()` agora chama `capturarLocalizacao()` de novo nesse caso
- `capturarLocalizacao()`: tentativa de alta precisão (GPS, `enableHighAccuracy:true`) agora usa timeout de 20s; se estourar, tenta automaticamente de novo via rede/Wi-Fi (`enableHighAccuracy:false`, mais rápido e mais confiável em ambiente fechado) antes de mostrar erro + botão "Tentar novamente" — pior caso ~40s em vez de uma única tentativa sem fallback
