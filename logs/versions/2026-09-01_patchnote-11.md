# 2026-09-01 — PatchNote 11

## Arquivos alterados
- `assets/js/shared.js`
- `assets/js/script.js`
- `assets/js/calendario.js`
- `assets/js/banco-horas.js`
- `assets/css/style.css`
- `views/calendario.html`
- `docs/CLAUDE.md`

## Itens/campos modificados

### Recibo do registro diário (redesign visual, várias iterações)
- `App.gerarComprovanteRegistro` (shared.js): recibo em Canvas com logo, título, foto centralizada, faixa cor do header (`#5F674F`, agora parte do mesmo card/tabela, sem elemento separado) com Data (esquerda, dia da semana maior que o resto da data, com vírgula "Segunda, 28/08/2026") e Usuário (direita); linha com Horário do ponto / Horário de saída / Saldo (3 colunas na mesma linha — Saldo só com sinal `+`/`-`/neutro sem sinal, nota "Tolerância até 15:10" em negrito preto abaixo do valor); Localização em linha única. Linhas divisórias (rótulo→valor, entre linhas, entre colunas) mais visíveis e espaçadas. Sem campo Período (removido a pedido).
- `App.formatarSaldoTexto(extraMin)` novo — formata só o resultado com sinal.

### Padronização e correção de nomenclatura
- Data com dia da semana usa vírgula em todo lugar (`getDataFmtHoje`, coluna "Dia" do relatório, `dataFmt` do Calendário, já existia em `formatarDataPrincipal`).
- `App.getPeriodoNome`/`comprovantePeriodo` agora mostra o intervalo real do período ("26/08/2026 – 25/09/2026") em vez do texto genérico "periodo 08" — corrige exibição em `da-periodo` (tela de detalhe do registro no Calendário).
- `getComprovanteNomePadrao` (shared.js) agora prefixa o nome do arquivo com a posição do dia dentro do período (`comprovante_01_...`, `comprovante_02_...`), calculada pela data do registro — não pela ordem em que foi de fato salvo, então preencher um dia esquecido depois não desorganiza a numeração.

### Navegação de período do Calendário unificada com Banco de Horas
- Lógica de período com registro (`getPeriodosDoAno`, `periodoTemRegistro`, `getPeriodosComRegistro`, `getSemestralLabel`) movida de `banco-horas.js` pra `shared.js`, reutilizada nos dois arquivos.
- Calendário agora navega pelos 12 períodos do ano civil, só mostrando/permitindo os que têm registro (mais o atual), igual ao Banco de Horas — antes travava numa janela rolante de 6 períodos de pagamento a partir do primeiro login.
- Trava de "não registrar antes do primeiro login" continua existindo, separada da navegação (`getPrimeiroPeriodoPermitido`, baseada em `getComprovanteCicloAtual`).

### Ícone do título "Registro" (Calendário) reage ao status do dia
- `#detailHeroIcon` (`views/calendario.html`) agora fica vermelho (`.is-danger`) quando o dia mostrado é desconto, verde (padrão) pra tempo extra/neutro — igual ao badge do card abaixo. Lógica em `mostrarDetalhe()` (calendario.js).

### Limpeza automática de dados do ano anterior (DESTRUTIVO)
- `limparDadosAnoAnteriorSeNecessario()` (shared.js, chamada em `initShell`): apaga permanentemente registros e baixas do ano civil anterior inteiro, automático e sem confirmação, a partir de 8 de janeiro do ano corrente. Roda uma vez por ano por usuário (flag em `localStorage`).
- `Store.removeMany`/`Store.removeBaixasMany` novos (Firestore batch em lotes de 450, ou remoção direta no `localStorage`).
