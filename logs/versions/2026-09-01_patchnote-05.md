# 2026-09-01 — PatchNote 05

## Arquivos alterados
- `index.html`
- `assets/css/style.css`
- `assets/js/script.js`

## Itens/campos modificados
- corrigido bug: `#geoStatus` (elemento usado por `exibirStatusGeo()` pra mostrar progresso/erro da geolocalização) não existia em nenhum HTML — todo status/erro de localização era gerado no JS mas nunca aparecia na tela. Adicionado `<div class="geo-status" id="geoStatus">` em `index.html`
- `capturarLocalizacao()` (script.js) agora checa `navigator.permissions.query({name:"geolocation"})` antes de tentar, pra distinguir "bloqueado nas permissões do navegador" de "serviço de localização do dispositivo desligado"
- em erro (permissão negada, timeout, indisponível), mostra botão "Tentar novamente" (`.geo-retry-btn`) e também tenta de novo sozinho quando a aba volta a ficar visível (`visibilitychange`) — evita falha única sem chance de retry
- CSS: `.geo-status`/`.geo-retry-btn` com contraste ajustado pro fundo do `.detail-hero.hero-muted` onde o status aparece
