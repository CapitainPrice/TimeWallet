# 2026-09-01 — PatchNote 06

## Arquivos alterados
- `index.html`
- `assets/css/style.css`
- `assets/js/script.js`
- `docs/CLAUDE.md`
- `docs/FIREBASE_SETUP.md`

## Itens/campos modificados
- consolidado o status de localização num único lugar: removido o `#geoStatus` separado (introduzido no patch anterior), status de progresso/erro/resultado passa a aparecer só no card "Localização" (`#comprovanteMeta`/`#comprovantePeriodo`), que agora também mostra "Obtendo localização..." antes de resolver, em vez de só o resultado final
- botão "Tentar novamente" (`#geoRetryBtn`) movido pra dentro do mesmo card, ligado uma única vez via `App.on` (antes era recriado a cada chamada de `exibirStatusGeo`)
- removida chamada duplicada de status em `carregarRegistroHoje` e código morto do antigo `#geoStatus` em `limparRegistroHojeUI`
- timeout do `getCurrentPosition` aumentado de 15s pra 30s, pra dar mais tempo em local com sinal de GPS fraco antes de cair em erro
- `docs/FIREBASE_SETUP.md`: schema Firestore corrigido (`endereco` → `address`, adicionado `comprovantePeriodo` e a coleção `baixas` que faltava), `storageBucket` atualizado pro formato novo (`.firebasestorage.app`), nota sobre aviso de depreciação do `enableMultiTabIndexedDbPersistence`
- `docs/CLAUDE.md`: descrição do fluxo de localização atualizada pra refletir o card único + retry
