# 2026-09-01 — PatchNote 10

## Arquivos alterados
- `assets/css/style.css`

## Itens/campos modificados
- corrigido bug visual: em telas ≤360px, `.period-sheet-actions{flex-direction:column;}` empilhava os botões "Cancelar"/"Salvar período" (e os do modal de seleção de horário, mesma classe) um em cima do outro em vez de lado a lado. Regra removida — os botões usam `flex:1` e ficam lado a lado em qualquer largura
